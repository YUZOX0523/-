# CLAUDE.md — Claude Code 完全ガイド（非エンジニア向け）

> このファイルは、Claude Code（AI アシスタント）がプロジェクトを理解するための設定ファイルであると同時に、
> **初めて触る人でも迷わない「教科書」** として書かれています。

---

## 目次

1. [そもそも Claude Code って何？](#1-そもそも-claude-code-って何)
2. [このプロジェクトの説明](#2-このプロジェクトの説明)
3. [環境のセットアップ（準備編）](#3-環境のセットアップ準備編)
4. [基本操作チートシート](#4-基本操作チートシート)
5. [よくある「やりたいこと」逆引き辞典](#5-よくあるやりたいこと逆引き辞典)
6. [トラブルが起きたときのQ&A](#6-トラブルが起きたときのqa)
7. [開発ルールとお約束](#7-開発ルールとお約束)
8. [用語集（わからない言葉はここ）](#8-用語集わからない言葉はここ)

---

## 1. そもそも Claude Code って何？

Claude Code は **「AIと会話しながらプログラムを作ったり直したりできるツール」** です。

### 普通のチャットAIとの違い

| 普通のチャットAI | Claude Code |
|---|---|
| 質問すると答えを「文章」で返す | 質問すると **実際にファイルを編集してくれる** |
| コードはコピペして自分で貼る | **自動でファイルに書き込んでくれる** |
| プロジェクト全体は見えない | **フォルダの中身を全部読める** |
| コマンド実行は自分でやる | **コマンドも代わりに実行してくれる** |

### たとえ話

> 料理でいうと——
> 普通のAI = 「レシピを教えてくれる人」
> Claude Code = 「キッチンに立って一緒に料理してくれる人」

---

## 2. このプロジェクトの説明

### 何をするアプリ？

**企業の info@ メールアドレスに営業メールを自動送信するアプリ** です。

### 技術スタック（使っている道具）

| 道具 | 役割 | たとえると… |
|---|---|---|
| **Node.js** | アプリの本体（エンジン） | 車のエンジン |
| **Express** | Webページを表示する仕組み | 車のハンドル |
| **SQLite (better-sqlite3)** | データを保存する箱 | 引き出し付きの棚 |
| **Nodemailer** | メールを送る仕組み | 郵便配達員 |
| **csv-parser** | CSVファイルを読む仕組み | Excelの読み取り係 |
| **Multer** | ファイルアップロードの仕組み | 受付窓口 |
| **dotenv** | 秘密の設定を管理 | 金庫 |

### フォルダ構成

```
/ （プロジェクトのルート = 一番上のフォルダ）
├── CLAUDE.md        ← 今読んでいるこのファイル
├── package.json     ← アプリの「材料リスト」（どんな道具を使うか）
├── sample.csv       ← 送信先リストのサンプル
├── data/            ← データベースが入る場所
├── src/             ← アプリの本体コード
│   ├── index.js     ← アプリの入口（起動するファイル）
│   ├── config.js    ← 設定ファイル
│   ├── db.js        ← データベース操作
│   ├── mailer.js    ← メール送信ロジック
│   ├── public/      ← ブラウザに表示する画面（HTML/CSS/JS）
│   └── routes/      ← URLごとの処理を書く場所
```

---

## 3. 環境のセットアップ（準備編）

### ステップ① 必要なものをインストールする

```bash
npm install
```

> **「npm install」って何？**
> 「材料リスト（package.json）に書いてある道具を全部ダウンロードしてね」という命令です。
> 料理でいうと「レシピに書いてある食材をスーパーで買ってくる」ようなものです。

### ステップ② 秘密の設定ファイルを作る

プロジェクトのルートに `.env` ファイルを作り、以下を書きます：

```
SMTP_HOST=（メールサーバーのアドレス）
SMTP_PORT=587
SMTP_USER=（メールアドレス）
SMTP_PASS=（パスワード）
```

> **注意：`.env` ファイルは「金庫」です。絶対にGitHubにアップしないでください。**
> パスワードが世界中に公開されてしまいます。

### ステップ③ アプリを起動する

```bash
npm start
```

開発中（ファイルを変更したら自動で再起動したいとき）：

```bash
npm run dev
```

---

## 4. 基本操作チートシート

### Claude Code への頼み方

Claude Code は **日本語で話しかけるだけ** で動きます。以下はそのまま使えるフレーズ集です。

#### ファイルを見たいとき

```
「src/index.js を見せて」
「config.js の中身を教えて」
「このプロジェクトのフォルダ構成を教えて」
```

#### コードを直してほしいとき

```
「メール送信のエラーを直して」
「ポート番号を 3000 から 8080 に変えて」
「CSVの読み込みで文字化けするのを直して」
```

#### 新しい機能を追加してほしいとき

```
「送信済みメールの一覧を表示するページを作って」
「メール送信前に確認ダイアログを出すようにして」
「CSVの代わりにExcelファイルも読めるようにして」
```

#### テスト・確認したいとき

```
「npm start でアプリが動くか確認して」
「エラーが出てるから原因を調べて」
「このコードに問題がないかチェックして」
```

#### Git 操作（バージョン管理）

```
「今の変更をコミットして」
「変更をプッシュして」
「今どのブランチにいるか教えて」
```

### スラッシュコマンド一覧

| コマンド | 意味 | いつ使う？ |
|---|---|---|
| `/help` | ヘルプを表示 | 困ったとき |
| `/commit` | 変更を保存（コミット） | 作業がひと区切りついたとき |
| `/clear` | 会話をリセット | 話題を変えたいとき |

---

## 5. よくある「やりたいこと」逆引き辞典

### 「メールのテンプレートを変えたい」

```
「mailer.js のメール本文のテンプレートを、以下の内容に変えて」
→ そのあとに変更したい本文を書く
```

### 「送信先のCSVの形式を変えたい」

```
「sample.csv を見せて。列の順番を変えたいんだけど」
→ Claude が現在の形式を見せてくれるので、希望を伝える
```

### 「画面のデザインを変えたい」

```
「src/public/ の中のHTMLを見せて。ボタンの色を青に変えたい」
```

### 「エラーが出て動かない！」

```
「npm start したらエラーが出た。これを貼るから原因を教えて」
→ エラーメッセージをそのまま貼り付ける
```

### 「新しいページを追加したい」

```
「/history というURLで送信履歴を見れるページを追加して」
```

### 「データベースの中身を確認したい」

```
「data/ フォルダにあるデータベースの中身を見せて」
```

---

## 6. トラブルが起きたときのQ&A

### Q. 「npm install がエラーになる」

**A.** 以下を Claude に伝えてください：
```
「npm install したらエラーが出た。エラーメッセージはこれ：（コピペ）」
```
よくある原因：
- Node.js がインストールされていない → 「Node.js をインストールする方法を教えて」
- ネットワークに繋がっていない → Wi-Fi を確認

### Q. 「メールが送信されない」

**A.** 確認ポイント：
1. `.env` ファイルの設定は正しいか → 「.env の設定を確認して」
2. メールサーバーの設定が合っているか → 「SMTP の設定を確認して」
3. パスワードが間違っていないか

### Q. 「画面が表示されない」

**A.**
```
「npm start した後、http://localhost:3000 にアクセスしても画面が出ない。原因を調べて」
```

### Q. 「間違えてファイルを消しちゃった」

**A.** Git を使っていれば戻せます：
```
「さっき消したファイルを元に戻して」
```

### Q. 「Claude が何を言っているかわからない」

**A.** 遠慮なくこう聞いてください：
```
「今の説明をもっと簡単に、専門用語なしで教えて」
「小学生でもわかるように説明して」
```

---

## 7. 開発ルールとお約束

### コードを書くときのルール

- **言語**：JavaScript（Node.js）
- **インデント**：スペース2つ
- **ファイル名**：小文字のケバブケース（例：`send-mail.js`）
- **コメント**：日本語OK

### Git（バージョン管理）のルール

- `master` ブランチには直接変更を入れない
- 作業は必ず **ブランチ** を作ってから行う（Claude Code が自動でやってくれます）
- コミットメッセージは **日本語OK**、何をしたか分かるように書く

> **ブランチって何？**
> 「作業用のコピー」のことです。
> ノートの原本をコピーして、コピーの方に書き込む→OK なら原本に反映、というイメージです。

### セキュリティのルール

- `.env` ファイルは **絶対に Git にコミットしない**（パスワードが漏れます）
- API キーやパスワードは **必ず `.env` に書く**（コードに直接書かない）
- 外部から受け取ったデータは **必ずチェック** してから使う

### Claude Code へのお願い

- コードを変更するときは **必ず理由を説明してから** 変更してください
- エラーが出たら **原因と対策を両方** 教えてください
- 専門用語を使うときは **かっこ書きで説明** を付けてください

---

## 8. 用語集（わからない言葉はここ）

| 用語 | 意味 | もっとカンタンに言うと |
|---|---|---|
| **npm** | Node.js の道具管理ツール | アプリの「App Store」みたいなもの |
| **package.json** | 使う道具のリスト | 料理の「材料リスト」 |
| **Git** | 変更履歴を記録するツール | ファイルの「タイムマシン」 |
| **コミット** | 変更を記録すること | ゲームの「セーブ」 |
| **プッシュ** | 記録をネット上に送ること | セーブデータを「クラウド」に保存 |
| **ブランチ** | 作業用の分岐コピー | ノートのコピーを作って試し書き |
| **マージ** | ブランチの変更を本体に反映 | 試し書きの内容を原本に清書 |
| **プルリクエスト (PR)** | 変更を反映していいか確認する仕組み | 「これでOKですか？」と上司に確認 |
| **API** | アプリ同士が会話する窓口 | レストランの「注文カウンター」 |
| **SMTP** | メール送信の通信ルール | 郵便局の「配達ルール」 |
| **CSV** | カンマ区切りのデータファイル | 超シンプルな Excel ファイル |
| **データベース (DB)** | データを整理して保存する箱 | Excel のすごい版 |
| **SQLite** | 軽量なデータベース | ファイル1個で動く小さなDB |
| **ポート番号** | アプリの「入口番号」 | マンションの「部屋番号」 |
| **localhost** | 自分のパソコンのこと | 「自分の家」のアドレス |
| **環境変数 (.env)** | 秘密の設定値 | 金庫に入れたメモ |
| **ルーティング** | URLと処理の紐づけ | 「この道を通ったらここに着く」という地図 |
| **ミドルウェア** | リクエストの途中処理 | 受付→担当者の間にいる「仲介役」 |
| **デプロイ** | アプリを本番環境に公開すること | お店の「オープン」 |

---

## さいごに

Claude Code は「完璧な指示」を必要としません。
**「こんな感じにしたい」「ここがおかしい」** くらいのざっくりした伝え方で大丈夫です。

分からないことがあったら、いつでも：

```
「これ何？」
「もっと簡単に説明して」
「最初からやり直したい」
```

と聞いてください。Claude Code はいつでもお手伝いします。
